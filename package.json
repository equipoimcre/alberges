{
  "name": "root",
  "private": true,
  "scripts": {
    "certificate": "node ./utils/certificate",
    "docker:prod": "docker-compose -f docker/prod/docker-compose.yml ",
    "docker:prod:up": "npm run docker:prod -- up"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "selfsigned": "^1.10.11"
  }
}
